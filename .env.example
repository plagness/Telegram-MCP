# ==============================================
# Telegram-MCP — Автономная конфигурация
# ==============================================
#
# Этот файл используется, когда Telegram-MCP запускается автономно.
#
# ВАЖНО: Если Telegram-MCP находится внутри более крупного проекта,
# токен бота будет автоматически взят из корневого .env (BOT_TOKEN).
# В этом случае TELEGRAM_BOT_TOKEN можно оставить пустым.
#
# Для автономного использования: скопируйте в .env и заполните.

# Telegram Bot API
TELEGRAM_BOT_TOKEN=your_bot_token_here
# Опционально: несколько токенов через запятую (для мультибота)
TELEGRAM_BOT_TOKENS=
TELEGRAM_API_BASE=https://api.telegram.org
# Опционально: чат по умолчанию для вызовов без chat_id
DEFAULT_CHAT_ID=

# Database (PostgreSQL)
DB_USER=telegram
DB_PASSWORD=telegram
DB_NAME=telegram
DB_HOST=tgdb
DB_PORT=5432
DB_DSN=postgresql://telegram:telegram@tgdb:5432/telegram

# Canonical host ports (preferred)
PORT_DB_TG=5436
PORT_HTTP_TGAPI=8081
PORT_MCP_TG=3335

# Legacy compatibility vars
API_PORT=8081
MCP_HTTP_PORT=3335

TELEGRAM_API_URL=http://tgapi:8000

# MCP Authentication (опционально)
MCP_HTTP_TOKEN=

# Logging
LOG_LEVEL=info

# Templates
TEMPLATE_AUTOSEED=1

# Web-UI (Telegram Mini App)
WEBUI_ENABLED=false
WEBUI_PUBLIC_URL=https://your-domain.example.com:8443
WEBUI_BOT_USERNAME=YourBotUsername
WEBUI_APP_NAME=app
PORT_WEBUI=8443
