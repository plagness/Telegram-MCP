# Roadmap: Расширение telegram-api

План внедрения новых возможностей Telegram Bot API на основе [официальной документации](https://core.telegram.org/bots/api) и [changelog](https://core.telegram.org/bots/api-changelog).

---

## Фаза 1: Интерактивность (высокий приоритет)

### 1.1 Опросы и викторины (Polls & Quizzes)

**Описание**: Нативные опросы Telegram с анонимным голосованием, множественным выбором, викторины с правильным ответом.

**API методы**:
- `POST /v1/polls/send` — создание опроса/викторины
- `POST /v1/polls/{message_id}/stop` — остановка опроса с показом результатов
- `GET /v1/polls/{poll_id}` — получение результатов опроса

**Параметры sendPoll**:
```json
{
  "chat_id": -100123456,
  "question": "Какой язык программирования вы предпочитаете?",
  "options": ["Python", "JavaScript", "Go", "Rust"],
  "is_anonymous": true,
  "type": "quiz",
  "correct_option_id": 0,
  "explanation": "Python — самый популярный язык для ML и data science",
  "open_period": 300,
  "allows_multiple_answers": false
}
```

**MCP-тулзы**:
- `polls.send` — создание опроса
- `polls.stop` — остановка опроса
- `polls.get_results` — получение результатов

**Use case для LLM**:
```
LLM: "Давайте спросим команду: какой фреймворк использовать?"
→ создаёт опрос через MCP
→ ждёт 5 минут
→ получает результаты
→ анализирует и предлагает решение
```

**БД**:
- Таблица `polls` (poll_id, question, options, type, correct_answer, results_json)
- Обновление из вебхуков (`poll`, `poll_answer`)

---

### 1.2 Расширенные inline-кнопки

**Что уже есть**: `callback_data`, `url`

**Что добавить**:

| Тип кнопки | Параметр | Описание |
|------------|----------|----------|
| `switch_inline_query` | str | Переход в inline-режим бота в другом чате |
| `switch_inline_query_current_chat` | str | Inline-режим в текущем чате |
| `switch_inline_query_chosen_chat` | object | Выбор чата для inline-запроса |
| `callback_game` | object | Запуск игры |
| `pay` | bool | Кнопка оплаты |
| `login_url` | object | OAuth авторизация через бота |
| `web_app` | object | Запуск Mini App |
| `copy_text` | object | Копирование текста в буфер |

**Обновление моделей** (api/app/models.py):
```python
class InlineKeyboardButton(BaseModel):
    text: str
    url: str | None = None
    callback_data: str | None = None
    web_app: WebAppInfo | None = None
    login_url: LoginUrl | None = None
    switch_inline_query: str | None = None
    switch_inline_query_current_chat: str | None = None
    switch_inline_query_chosen_chat: SwitchInlineQueryChosenChat | None = None
    callback_game: CallbackGame | None = None
    pay: bool | None = None
    copy_text: CopyTextButton | None = None
```

---

### 1.3 Реакции (Reactions)

**Описание**: Установка реакций на сообщения (эмодзи, платные Star-реакции).

**API методы**:
- `POST /v1/messages/{message_id}/reaction` — установить реакцию
- `GET /v1/messages/{message_id}/reactions` — список реакций

**Параметры setMessageReaction**:
```json
{
  "chat_id": -100123456,
  "message_id": 42,
  "reaction": [
    {"type": "emoji", "emoji": "👍"},
    {"type": "custom_emoji", "custom_emoji_id": "12345"}
  ],
  "is_big": false
}
```

**MCP-тулза**: `messages.set_reaction`

**Use case**: LLM может реагировать на сообщения пользователей (👍/👎), показывая прогресс обработки.

---

## Фаза 2: Форумы и топики (средний приоритет)

### 2.1 Управление топиками

**Описание**: Создание и управление топиками в forum-супергруппах.

**API методы**:
- `POST /v1/chats/{chat_id}/topics` — создание топика
- `POST /v1/chats/{chat_id}/topics/{topic_id}/edit` — редактирование
- `DELETE /v1/chats/{chat_id}/topics/{topic_id}` — удаление
- `POST /v1/chats/{chat_id}/topics/{topic_id}/close` — закрытие
- `POST /v1/chats/{chat_id}/topics/{topic_id}/reopen` — открытие
- `GET /v1/chats/{chat_id}/topics/icon-stickers` — доступные иконки

**Параметры createForumTopic**:
```json
{
  "chat_id": -100123456,
  "name": "Техническое обсуждение",
  "icon_color": 0x6FB9F0,
  "icon_custom_emoji_id": "5312536423851630001"
}
```

**Поддержка message_thread_id**:
- Уже реализовано в `sendMessage` (`message_thread_id` параметр)
- Добавить в SDK метод `send_to_topic(chat_id, topic_id, text)`

**MCP-тулзы**:
- `topics.create`
- `topics.edit`
- `topics.delete`
- `topics.close` / `topics.reopen`

---

### 2.2 Топики в личных чатах

**Новая функция Bot API 9.3**: форум-режим для личных чатов.

**Поддержка**:
- Отправка сообщений в топики личных чатов
- `sendMessageDraft` для потоковой генерации (draft bubbles)

---

## Фаза 3: Контент (средний приоритет)

### 3.1 Стикеры

**API методы**:
- `POST /v1/media/send-sticker` — отправка стикера
- `POST /v1/stickers/upload` — загрузка файла стикера
- `POST /v1/stickers/sets` — создание набора стикеров
- `POST /v1/stickers/sets/{name}/stickers` — добавление стикера в набор
- `DELETE /v1/stickers/sets/{name}` — удаление набора

**MCP-тулзы**:
- `media.send_sticker`
- `stickers.create_set`
- `stickers.add_to_set`

---

### 3.2 Кастомные эмодзи

**Описание**: Отправка сообщений с премиальными кастомными эмодзи.

**Ограничения**: Только боты с купленными username на Fragment.

**Реализация**:
- Поддержка в `parse_mode=HTML`: `<tg-emoji emoji-id="ID">🔥</tg-emoji>`
- MessageEntity type = `custom_emoji` с `custom_emoji_id`

**API**:
- `GET /v1/custom-emoji/{emoji_ids}` — получение информации об эмодзи
- `GET /v1/custom-emoji/stickers` — список доступных наборов

---

### 3.3 Игры

**API методы**:
- `POST /v1/games/send` — отправка игры
- `POST /v1/games/{game_short_name}/score` — установка счёта
- `GET /v1/games/{game_short_name}/high-scores` — таблица лидеров

**Use case**: LLM может создавать интерактивные квизы/игры для обучения.

---

## Фаза 4: Telegram Stars и монетизация (низкий приоритет)

### 4.1 Платные медиа

**Описание**: Отправка фото/видео за Telegram Stars.

**API методы**:
- `POST /v1/media/send-paid-photo` — платное фото
- `POST /v1/media/send-paid-video` — платное видео

**Параметры**:
```json
{
  "chat_id": -100123456,
  "star_count": 100,
  "media": [
    {"type": "photo", "media": "file_id_or_url"}
  ]
}
```

---

### 4.2 Подписки и инвойсы

**API методы**:
- `POST /v1/invoices/send` — создание инвойса
- `POST /v1/invoices/link` — создание ссылки на оплату
- `POST /v1/stars/refund` — возврат Stars
- `GET /v1/stars/transactions` — история транзакций Stars
- `GET /v1/stars/balance` — баланс бота

**MCP-тулзы**:
- `invoices.send`
- `stars.get_balance`
- `stars.get_transactions`

---

### 4.3 Гифты и награды

**API методы**:
- `POST /v1/gifts/send` — отправка подарка Premium
- `GET /v1/gifts/inventory` — инвентарь подарков
- `POST /v1/gifts/upgrade` — апгрейд подарка за Stars
- `POST /v1/giveaways/send` — создание розыгрыша
- `GET /v1/giveaways/{id}/winners` — победители розыгрыша

---

## Фаза 5: Бизнес-функции (низкий приоритет)

### 5.1 Business Accounts

**Описание**: Управление бизнес-аккаунтами от имени бота.

**API методы**:
- `GET /v1/business/connection` — информация о подключении
- `POST /v1/business/profile` — управление профилем (имя, био, username)
- `POST /v1/business/photo` — изменение фото профиля
- `GET /v1/business/stars/balance` — баланс Stars бизнес-аккаунта

**MCP-тулзы**: `business.get_connection`, `business.update_profile`

---

### 5.2 Stories

**Описание**: Публикация stories от имени бизнес-аккаунта.

**API методы**:
- `POST /v1/stories/send` — создание story
- `POST /v1/stories/{id}/edit` — редактирование
- `DELETE /v1/stories/{id}` — удаление
- `POST /v1/stories/{id}/repost` — репост story

**Параметры**:
```json
{
  "chat_id": -100123456,
  "media": {"type": "photo", "media": "file_id"},
  "duration": 86400,
  "privacy_settings": {"type": "public"},
  "active_areas": [
    {"type": "location", "x": 0.5, "y": 0.5}
  ]
}
```

---

### 5.3 Чек-листы

**Описание**: Отправка чек-листов от имени бизнес-аккаунта (Bot API 9.1).

**API методы**:
- `POST /v1/checklists/send` — отправка чек-листа
- `POST /v1/checklists/{message_id}/edit` — редактирование

**Параметры**:
```json
{
  "chat_id": -100123456,
  "items": [
    {"text": "Подготовить презентацию", "checked": false},
    {"text": "Отправить отчёт", "checked": true}
  ]
}
```

---

## Фаза 6: Inline-режим и Mini Apps (низкий приоритет)

### 6.1 Inline-запросы

**API методы**:
- `POST /v1/inline/answer` — ответ на inline-запрос
- `GET /v1/inline/queries` — история inline-запросов

**MCP-тулза**: `inline.answer`

---

### 6.2 Mini Apps (Web Apps)

**Описание**: Запуск веб-приложений внутри Telegram.

**API методы**:
- `POST /v1/web-apps/answer-query` — ответ на запрос от Web App
- `GET /v1/web-apps/queries` — список запросов

**Кнопки**:
```json
{
  "text": "Открыть приложение",
  "web_app": {"url": "https://example.com/app"}
}
```

---

## Приоритеты внедрения

| Фаза | Приоритет | Сложность | Ценность для LLM |
|------|-----------|-----------|------------------|
| **1. Интерактивность** | 🔥 Высокий | Средняя | ⭐⭐⭐⭐⭐ |
| 2. Форумы и топики | ⚠️ Средний | Низкая | ⭐⭐⭐⭐ |
| 3. Контент | ⚠️ Средний | Средняя | ⭐⭐⭐ |
| 4. Stars и монетизация | 🟢 Низкий | Высокая | ⭐⭐ |
| 5. Бизнес-функции | 🟢 Низкий | Высокая | ⭐⭐ |
| 6. Inline и Mini Apps | 🟢 Низкий | Очень высокая | ⭐ |

---

## Рекомендации по внедрению

### Первоочередные задачи (следующая итерация)

1. **Опросы** — наибольшая ценность для LLM (сбор мнений команды, викторины)
2. **Расширенные inline-кнопки** — `web_app`, `switch_inline_query`, `copy_text`
3. **Реакции** — простая реализация, high UX impact
4. **Форум-топики** — базовая поддержка уже есть (`message_thread_id`), нужны методы управления

### Архитектурные изменения

- **Вебхуки**: Добавить обработку `poll`, `poll_answer`, `message_reaction`, `forum_topic_*`
- **БД**: Новые таблицы `polls`, `reactions`, `forum_topics`
- **SDK**: Методы `send_poll()`, `set_reaction()`, `create_topic()`
- **MCP**: 15-20 новых тулзов в фазах 1-2

### Тестирование

- Создать `scripts/test_polls.py`, `test_reactions.py`, `test_topics.py`
- Проверить через MCP HTTP API
- Smoke-тест всех inline-кнопок

---

## Источники

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Bot API Changelog](https://core.telegram.org/bots/api-changelog)
- [Inline Keyboard Buttons](https://core.telegram.org/bots/2-0-intro)
- [Forum Topics](https://core.telegram.org/api/forum)
- [Message Reactions](https://core.telegram.org/api/reactions)
- [Custom Emoji](https://core.telegram.org/api/custom-emoji)
