# MCP-инструменты

MCP-сервер предоставляет 71 инструмент для интеграции с LLM.

Работает по двум протоколам:
- **stdio** — стандартный MCP-транспорт для Claude и других LLM
- **HTTP** — REST API на порту 3335

## HTTP API

Базовый URL: `http://<host>:3335`

### Эндпоинты

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/health` | Проверка работоспособности |
| GET | `/tools` | Список всех инструментов |
| POST | `/tools/:name` | Вызов инструмента |

### Аутентификация

Если задана переменная `MCP_HTTP_TOKEN`, все запросы к `/tools` требуют Bearer-токен:

```bash
curl http://127.0.0.1:3335/tools \
  -H "Authorization: Bearer your-token"
```

## Список инструментов

### Сообщения

| Инструмент | Описание |
|------------|----------|
| `messages.send` | Отправить текстовое сообщение (текст или шаблон) |
| `messages.edit` | Редактировать сообщение по внутреннему ID |
| `messages.delete` | Удалить сообщение по внутреннему ID |
| `messages.fetch` | Получить сообщения с фильтрацией |

### Медиа

| Инструмент | Описание |
|------------|----------|
| `media.send_photo` | Отправить фото (URL или file_id) |
| `media.send_document` | Отправить документ/файл |

### Опросы

| Инструмент | Описание |
|------------|----------|
| `polls.send` | Создать опрос или викторину с правильным ответом |
| `polls.stop` | Остановить опрос и показать результаты |
| `polls.list` | Получить список опросов с фильтрацией |

### Реакции

| Инструмент | Описание |
|------------|----------|
| `reactions.set` | Установить эмодзи-реакцию на сообщение |

### Шаблоны

| Инструмент | Описание |
|------------|----------|
| `templates.list` | Список Jinja2-шаблонов |
| `templates.render` | Отрендерить шаблон с переменными |

### Команды

| Инструмент | Описание |
|------------|----------|
| `commands.create` | Создать набор команд для скоупа |
| `commands.list` | Получить все наборы команд |
| `commands.sync` | Синхронизировать с Telegram |

### Callback Queries

| Инструмент | Описание |
|------------|----------|
| `callbacks.answer` | Ответить на нажатие inline-кнопки |
| `callbacks.list` | Список полученных callback queries |

### Чаты

| Инструмент | Описание |
|------------|----------|
| `chats.get` | Информация о чате |
| `chats.member` | Статус участника чата |

### Вебхук

| Инструмент | Описание |
|------------|----------|
| `webhook.setup` | Настроить вебхук |
| `webhook.info` | Текущая конфигурация вебхука |
| `webhook.delete` | Удалить вебхук |

### Обновления

| Инструмент | Описание |
|------------|----------|
| `updates.fetch` | Получить входящие обновления |

### Предсказания

| Инструмент | Описание |
|------------|----------|
| `predictions.create_event` | Создать событие для предсказаний |
| `predictions.list` | Список событий |
| `predictions.get` | Получить событие по ID |
| `predictions.place_bet` | Разместить предсказание |
| `predictions.resolve` | Разрешить событие |
| `predictions.user_bets` | Предсказания пользователя |
| `predictions.auto_resolve` | Автоматическое разрешение через LLM |

### Stars & Баланс

| Инструмент | Описание |
|------------|----------|
| `stars.invoice` | Создать счёт на оплату Stars |
| `stars.refund` | Возврат платежа |
| `stars.transactions` | История транзакций |
| `stars.balance` | Баланс звёзд бота |
| `stars.gifts_premium` | Подарок премиум-подписки |
| `stars.gifts_user` | Подарки пользователя |
| `stars.gifts_chat` | Подарки чата |
| `stars.stories_repost` | Репост истории |
| `balance.get` | Баланс пользователя (виртуальная валюта) |
| `balance.credit` | Пополнить баланс |
| `balance.debit` | Списать с баланса |
| `balance.currencies` | Список валют |

### Чеклисты

| Инструмент | Описание |
|------------|----------|
| `checklists.send` | Отправить чек-лист |
| `checklists.edit` | Редактировать чек-лист |

### Календарь

| Инструмент | Описание |
|------------|----------|
| `calendar.create` | Создать календарь |
| `calendar.list` | Список календарей |
| `calendar.create_entry` | Создать запись в календаре |
| `calendar.list_entries` | Список записей (с фильтрацией по дате, статусу, тегам) |
| `calendar.get_entry` | Получить запись по ID |
| `calendar.get_chain` | Получить цепочку связанных событий (parent/children) |
| `calendar.update_entry` | Обновить запись (title, emoji, priority, tags, status) |
| `calendar.move_entry` | Переместить запись (изменить start_at/end_at) |
| `calendar.set_status` | Установить статус (active, done, cancelled, archived) |
| `calendar.delete_entry` | Удалить запись |
| `calendar.entry_history` | История изменений записи |
| `calendar.bulk_create` | Массовое создание записей (до 100) |
| `calendar.bulk_delete` | Массовое удаление записей (до 100) |
| `calendar.upcoming` | Предстоящие активные события |

### Web-UI

| Инструмент | Описание |
|------------|----------|
| `webui.create_page` | Создать страницу (page/survey/prediction/calendar) |
| `webui.list_pages` | Список страниц |
| `webui.create_link` | Создать индивидуальную ссылку |
| `webui.get_submissions` | Получить ответы формы |
| `webui.create_prediction` | Создать страницу предсказания (shortcut) |
| `webui.create_survey` | Создать опросник (shortcut) |

### Бот

| Инструмент | Описание |
|------------|----------|
| `bot.info` | Информация о боте (имя, username) |

## Примеры вызовов

### Отправка сообщения

```bash
curl -X POST http://127.0.0.1:3335/tools/messages.send \
  -H 'content-type: application/json' \
  -d '{
    "chat_id": -100123456,
    "text": "<b>Привет от LLM!</b>",
    "parse_mode": "HTML"
  }'
```

### Отправка фото

```bash
curl -X POST http://127.0.0.1:3335/tools/media.send_photo \
  -H 'content-type: application/json' \
  -d '{
    "chat_id": -100123456,
    "photo": "https://example.com/img.jpg",
    "caption": "Описание"
  }'
```

### Создание команд

```bash
curl -X POST http://127.0.0.1:3335/tools/commands.create \
  -H 'content-type: application/json' \
  -d '{
    "scope_type": "default",
    "commands": [
      {"command": "start", "description": "Начать"},
      {"command": "help", "description": "Справка"}
    ]
  }'
```

### Получение обновлений

```bash
curl -X POST http://127.0.0.1:3335/tools/updates.fetch \
  -H 'content-type: application/json' \
  -d '{"limit": 10, "update_type": "message"}'
```

### Создание опроса

```bash
curl -X POST http://127.0.0.1:3335/tools/polls.send \
  -H 'content-type: application/json' \
  -d '{
    "chat_id": -100123456,
    "question": "Какой язык программирования вы предпочитаете?",
    "options": ["Python", "JavaScript", "Go", "Rust"],
    "type": "quiz",
    "correct_option_id": 0,
    "explanation": "Python — самый популярный язык для ML и data science",
    "open_period": 300
  }'
```

### Установка реакции

```bash
curl -X POST http://127.0.0.1:3335/tools/reactions.set \
  -H 'content-type: application/json' \
  -d '{
    "chat_id": -100123456,
    "message_id": 42,
    "reaction": [{"type": "emoji", "emoji": "👍"}],
    "is_big": false
  }'
```

## Подключение к Claude

В `claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "telegram": {
      "command": "node",
      "args": ["/path/to/Telegram-MCP/mcp/dist/index.js"],
      "env": {
        "TELEGRAM_API_URL": "http://localhost:8081",
        "MCP_HTTP_PORT": "3335"
      }
    }
  }
}
```

Или через Docker:

```json
{
  "mcpServers": {
    "telegram": {
      "command": "docker",
      "args": ["exec", "-i", "tgmcp", "node", "dist/index.js"]
    }
  }
}
```

## Архитектура

```
LLM (Claude)
    │ MCP stdio
    ▼
telegram-mcp / tgmcp (Node.js :3335)
    │ HTTP
    ▼
tgapi (FastAPI :8081)
    │ httpx
    ▼
Telegram Bot API
```

MCP-сервер — тонкий прокси. Он не содержит бизнес-логики: валидирует параметры (zod) и перенаправляет запросы к HTTP API.

По умолчанию MCP использует `http://tgapi:8000`. Если URL не задан явно и основной хост недоступен,
на 1 релиз включён legacy retry на `http://telegram-api:8000`.
